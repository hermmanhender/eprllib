# This workflow will upload a Python Package using Twine when a release is created
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python#publishing-to-package-registries

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: natural_ventilation_EP_RLlib

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  deploy:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.10'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build
        pip install ray[all]
        pip install torch
        pip install tensorflow
        pip install gymnasium
        pip install bayesian-optimization
        pip install numpy
    - name: Build package
      run: python -m build
    - name: natural_ventilation_EP_RLlib
      uses: pypa/gh-action-pypi-publish@27b31702a0e7fc50959f5ad993c78deac1bdfc29
      with:
        user: __token__
        password: pypi-AgEIcHlwaS5vcmcCJGQ5OGMxZDhhLWNmZTEtNDNiNS04NTZhLWY2OTJjNzQyNDllZAACKlszLCIwZmNlNzQxNi1iYjkxLTQzY2EtOTk4Mi1iZjJhNWQwZGZlNTQiXQAABiAgy1U3PtKkuFZo__wHFckDHsV_Ts_IY66lWwVnWw-GdQ
